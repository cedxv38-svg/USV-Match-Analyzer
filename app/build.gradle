plugins {
    id 'com.android.application'
}

android {
    namespace 'com.usv.matchanalyzer'
    compileSdk 35

    defaultConfig {
        applicationId 'com.usv.matchanalyzer.v2'
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName '1.1'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
